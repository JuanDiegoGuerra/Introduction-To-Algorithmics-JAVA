package semana10;

import java.awt.EventQueue;

import javax.swing.JFrame;
import javax.swing.JPanel;
import javax.swing.border.EmptyBorder;
import javax.swing.JLabel;
import javax.swing.JButton;

import java.awt.event.ActionListener;
import java.awt.event.ActionEvent;

import javax.swing.JTextField;
import javax.swing.JScrollPane;
import javax.swing.JTextArea;
import javax.swing.UIManager;
import javax.swing.JComboBox;
import javax.swing.DefaultComboBoxModel;

import java.awt.Font;

public class Juego extends JFrame implements ActionListener {

	// Declaración de variables
	private static final long serialVersionUID = 9206324162700448001L;
	private JPanel contentPane;
	private JLabel lblArco;
	private JLabel lblGoles;
	private JComboBox<String> cboArco;
	private JTextField txtGoles;
	private JButton btnProcesar;
	private JButton btnBorrar;
	private JScrollPane scpScroll;
	private JTextArea txtS;

	int arco, goles, puntaje;
	String premio;

	// Lanza la aplicación
	public static void main(String[] args) {
		try {
			UIManager.setLookAndFeel(UIManager.getSystemLookAndFeelClassName());
		}
		catch (Throwable e) {
			e.printStackTrace();
		}
		EventQueue.invokeLater(new Runnable() {
			public void run() {
				try {
					Juego frame = new Juego();
					frame.setVisible(true);
				}
				catch (Exception e) {
					e.printStackTrace();
				}
			}
		});
	}

	// Crea la GUI
	public Juego() {
		setTitle("Juego");
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setBounds(100, 100, 450, 300);

		contentPane = new JPanel();
		contentPane.setBorder(new EmptyBorder(5, 5, 5, 5));
		setContentPane(contentPane);
		contentPane.setLayout(null);

		lblArco = new JLabel("Arco");
		lblArco.setBounds(10, 11, 80, 14);
		contentPane.add(lblArco);

		lblGoles = new JLabel("Goles");
		lblGoles.setBounds(10, 36, 80, 14);
		contentPane.add(lblGoles);

		cboArco = new JComboBox<String>();
		cboArco.setModel(new DefaultComboBoxModel<String>(new String[] {"Peque\u00F1o", "Mediano", "Grande", "Extra Grande"}));
		cboArco.setBounds(100, 8, 100, 20);
		contentPane.add(cboArco);

		txtGoles = new JTextField();
		txtGoles.setBounds(100, 33, 100, 20);
		contentPane.add(txtGoles);
		txtGoles.setColumns(10);

		btnProcesar = new JButton("Procesar");
		btnProcesar.addActionListener(this);
		btnProcesar.setBounds(335, 7, 89, 23);
		contentPane.add(btnProcesar);

		btnBorrar = new JButton("Borrar");
		btnBorrar.addActionListener(this);
		btnBorrar.setBounds(335, 32, 89, 23);
		contentPane.add(btnBorrar);

		scpScroll = new JScrollPane();
		scpScroll.setBounds(10, 61, 414, 190);
		contentPane.add(scpScroll);

		txtS = new JTextArea();
		txtS.setFont(new Font("Monospaced", Font.PLAIN, 12));
		scpScroll.setViewportView(txtS);
	}

	// Direcciona eventos de tipo ActionEvent
	public void actionPerformed(ActionEvent arg0) {
		if (arg0.getSource() == btnBorrar) {
			actionPerformedBtnBorrar(arg0);
		}
		if (arg0.getSource() == btnProcesar) {
			actionPerformedBtnProcesar(arg0);
		}
	}

	// Procesa la pulsación del botón Borrar
	protected void actionPerformedBtnBorrar(ActionEvent arg0) {
		txtGoles.setText("");
		txtS.setText("");
		cboArco.setSelectedIndex(0);
		txtGoles.requestFocus();
	}

	// Procesa la pulsación del botón Procesar
	protected void actionPerformedBtnProcesar(ActionEvent arg0) {
        ingresarDatos();
        calcularPuntajeTotal();
        determinarPremio();
        mostrarResultados();
    }
	
	void imprimir(String cad){
		txtS.append(cad + "\n");
	}
    
    void ingresarDatos(){
		arco = cboArco.getSelectedIndex();
		goles = Integer.parseInt(txtGoles.getText());
    }
    
    void calcularPuntajeTotal(){
		switch(arco){
			case  0: puntaje = 6*goles; break;				
			case  1: puntaje = 4*goles; break;				
			case  2: puntaje = 3*goles; break;				
			default: puntaje = 2*goles;
		}
    }
    
    void determinarPremio(){
		if(puntaje < 6)
			premio = "Ninguno";
		else if(puntaje < 12)	
			premio = "Cuaderno";
		else if(puntaje < 18)	
			premio = "Peluche";
		else
			premio = "Reloj";	
    }
    
    void mostrarResultados(){
		txtS.setText("");
    	imprimir("Puntaje total : " + puntaje);
		imprimir("Premio        : " + premio);
    }
}